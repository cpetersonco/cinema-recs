__pycache__/
*.pyc
.venv/
.env
*.db
.claude/.credentials.json
dist/
build/
*.egg-info/
.pytest_cache/
.ruff_cache/
# graphify-out/graph.json (and the report/html/manifest alongside it) are
# committed so `git worktree` checkouts have the knowledge graph without
# re-running extraction; only its local rebuild cache is ignored.
graphify-out/cache/
graphify-out/.graphify_cached.json
graphify-out/.graphify_uncached.txt
graphify-out/.graphify_semantic*.json
